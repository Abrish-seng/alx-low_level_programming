#include <stdio.h>

int main(void)
{
	int n;
	if(n>0)
	{
		printf(a,":is positive %d\n");
	}
	if(n==0)
        {
                printf(a,":is zero %d\n");
        }
        if(n<0)
        {
                printf(a,":is negetive %d\n");
        }   
	return (0);
}
