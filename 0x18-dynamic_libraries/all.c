#include <stdio.h>
#include <stdlib.h>
/**
 * add - add two integer
 * @a: first integer
 * @b: second integer
 * Return: addition
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * divn - divition of two integers
 * @a: first integer
 * @b: second integer
 * Return: divtion 
 */
int divn(int a, int b)
{
	return (a / b);
}

/**
 * mod - mod of two integers
 * @a: first integer
 * @b: second integer
 * Return: mod of numbers
 */
 int mod(int a, int b)
 {
	 return (a % b);
 }

/**
 * mul - multiplication of two integers
 * @a: first integer
 * @b: second integer
 * Return: mul of numbers
 */
 int mul(int a, int b)
 {
	 return (a * b);
 }


/**
 * sub - subtruction of two integers
 * @a: first integer
 * @b: second integer
 * Return: additin
 */
 int sub(int a, int b)
 {
	 return (a - b);
 }
